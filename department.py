

### Create a class named Department, use the __init__() function to assign values for department name  and department id
class Department:
  def __init__(self, dept_name, dept_id):
    self.dept_name = dept_name
    self.dept_id = dept_id